# Task 05 - Построение дерева каталогов (рекурсивный обход)

## Постановка задачи

Программа рекурсивно создаёт **JSON-структуру каталога и файлов**.  
Каждый узел содержит имя, тип (`file` или `dir`) и список дочерних элементов (`children`), если это директория.

---

## Входные данные

- **Текстовый файл** с путём к корневой директории (например, `dir_root.txt`).  
  Пример содержимого:

