# Task 07 - Конвертация CSV -> JSON

## Постановка задачи

Программа читает CSV-файл с заголовком и преобразует его в JSON-массив объектов.  
Для каждого значения выполняется:

- удаление лишних пробелов;  
- конвертация чисел в integer или float, если возможно.

---

## Входные данные

- **CSV-файл** с заголовком.  
Пример `input.csv`:

