# Task 08 - Конвертация XML -> YAML

## Постановка задачи

Программа читает **XML-файл** и конвертирует его в **YAML**.  
Для каждого элемента выполняется рекурсивное преобразование в Hash.  
Повторяющиеся элементы с одинаковым именем собираются в массив.

---

## Входные данные

- **XML-файл**.  
Пример `input.xml`:

